# 다독임 작업 지침

## 범위와 읽기 순서

- `다독임`은 `inside-me`와 별개의 독립 프로젝트다.
- 핵심 기능은 A. 수면 유도 소리, B. 긍정 확언, C. 멍 때리기 소리다.
- 요구사항 논의와 개발은 `C → B → A` 순서로 진행한다.
- 기본 작업 루트는 이 파일이 있는 `다독임` 폴더다. 사용자가 명시적으로 요청하지 않으면 상위 `Playground`나 형제 프로젝트를 검색·수정·작업 범위에 포함하지 않는다.
- `docs/README.md`에서 현재 상태와 작업별 경로를 먼저 확인한다. 관련 제목·`REQ-*`·`DEC-*`·`CHG-*`를 검색해 필요한 범위만 읽고, 전체 감사가 아니면 모든 문서나 로그 전체를 읽지 않는다.

## 결정·문서·로그

- 확정 요구사항은 `docs/requirements.md`, 미확정 항목은 `docs/open-questions.md`, 결정은 `docs/decision-log.md`, 실제 변경은 `docs/change-log.md`에 둔다. 상세 절차는 `docs/development-workflow.md`, PR은 `docs/pull-request-guide.md`, 검수는 `docs/review-guide.md`를 따른다.
- 사용자 확정, Codex 구체화 초안·제안과 미정을 구분하고 사용자 표현을 왜곡하지 않는다. 사용자가 고르지 않은 제안을 확정·필수·완료로 기록하거나 구현하지 않는다.
- 중요한 선택은 한 번에 하나씩 2~3개 선택지와 추천 이유를 설명한다. 확정하면 요구사항·DEC·CHG와 `open-questions.md`를 함께 갱신하고, 제외·연기는 DEC·CHG에 이유를 남긴 뒤 활성 목록에서 제거한다.
- 모든 사용자 결정과 실제 문서·코드·설정 변경을 누적 기록한다. 기존 결정을 덮어쓰지 않고 새 DEC에서 변경 이유와 이전 결정을 연결한다.
- DEC와 CHG는 `의도 → 검토·작업 → 결정·결과 → 해석 → 다음 행동`을 짧게 기록하고 관련 요구사항·PR·커밋·검증을 연결한다. 대화 전문, 대량 출력과 중복 배경은 복사하지 않는다.
- 루트 지침에는 반복해서 적용할 필수 규칙만 둔다. 세부 설명은 관련 문서 한 곳에 두고, 기능별 규칙이 반복될 때만 해당 코드 가까이에 하위 `AGENTS.md`를 둔다.

## 개발과 검토

- 코드는 A·B·C 기능 중심 폴더로 나누고, 각 기능 안에서 View·ViewModel·Model 역할을 분리하며 상태는 `사용자 행동 → 상태 변경 → 화면 갱신`의 단방향으로 흐르게 한다.
- 코드·기능 PR은 사용자가 동작과 diff를 한 조각씩 이해하고 검증할 수 있는 작은 세로 조각으로 나눈다. 가능하면 사용자 동작 하나 또는 결함 하나를 기준으로 하고, 무관한 기능·대규모 리팩토링·일괄 포맷 변경을 섞지 않는다.
- 문서 전용 PR은 같은 결정이나 검토 주제에 필요한 요구사항·결정 로그·변경 로그·가이드를 한 묶음으로 올릴 수 있다. 파일·문단 수만으로 기계적으로 쪼개지 않되 서로 무관한 주제는 분리한다.
- 작업이 끝나면 변경 파일, 사용자 행동 변화, 검증 결과와 미확인 사항을 보여준다. 관련 포맷·린트·타입 검사·테스트·빌드·실기기 검증 중 실제 실행한 결과만 완료로 기록한다.
- 이름과 역할을 명확히 하고 중복·숨은 부수 효과·불필요한 추상화를 피한다. 동작 보존 리팩토링은 관련 테스트와 함께 하고 가능하면 기능 구현과 분리한다.
- GitHub 변경은 `codex/` 브랜치의 PR로 제출하고 `main`에 직접 푸시하지 않는다. `.github/pull_request_template.md`를 실제 결과로 채우며 병합은 사용자가 diff와 검증을 확인한 뒤 결정한다.
- GitHub Issue 생성과 배포는 사용자의 명시적 요청이나 승인 후에만 수행한다. 병렬화·CI/CD·상세 검수 절차는 연결된 가이드를 필요한 작업에서만 읽는다.
- 검수자는 구현 의도를 선의로 보완하지 않는 깐깐한 관리자로서 확정 요구사항, diff와 실행 증거를 대조한다. 증거 부족이나 불일치는 반려하고 교정 후 같은 기준으로 재검수한다.

## Code Review Rules

- 사용자 확정 `REQ-*`·`DEC-*`와 PR diff를 대조한다. 미확정 제안을 구현했거나 PR 목적 밖 변경을 섞었으면 승인하지 않는다. 안전한 경로는 변경을 분리하고 사용자 확인 후 다시 제출하는 것이다.
- PR 설명과 체크박스를 검증 증거로 간주하지 않는다. 실제 CI 결과, 실행 명령과 결과, 필요한 Android 실기기 결과를 확인한다. 필수 검증이 누락되거나 실패했으면 완료로 승인하지 않는다.
- 개인정보 노출, 데이터 손실·중복, 오디오 중단·백그라운드 재생 회귀, 근거 없는 테스트 약화를 우선 확인한다. 발견 사항은 위치·재현 조건·영향·안전한 수정 방향과 함께 보고하고, 수정 후 같은 기준으로 재검수한다.
