# 다독임 작업 지침

## 핵심 범위

- `다독임`은 `inside-me`와 별개의 독립 프로젝트다.
- 핵심 기능은 A. 수면 유도 소리, B. 긍정 확언, C. 멍 때리기 소리다.
- 요구사항 논의와 개발은 `C → B → A` 순서로 진행한다.
- 기본 작업 루트는 이 파일이 있는 `다독임` 폴더다. 사용자가 명시적으로 요청하지 않으면 상위 `Playground`나 형제 프로젝트를 검색·수정·작업 범위에 포함하지 않는다.
- 항상 `docs/README.md`를 먼저 읽고, 그 문서가 지정한 현재 작업 관련 문서만 추가로 읽는다.

## 문서와 로그

- 요구사항 변경은 `docs/requirements.md`, 사용자의 모든 제품·UX·기술·데이터·운영 결정은 `docs/decision-log.md`, 실제 파일·코드·설정 변경은 `docs/change-log.md`에 같은 작업 단위에서 반영한다.
- `docs/open-questions.md`를 미확정 사항의 임시 단일 목록으로 유지한다. 현재 질문 하나에만 2~3개 선택지와 추천을 펼치고, 나머지는 제목만 둔다.
- 사용자가 항목을 확정하면 관련 DEC와 요구사항을 같은 작업 단위에서 갱신한 뒤 임시 목록에서 제거한다. 제외·연기도 DEC를 남기고 제거하며, 모든 항목이 처리되면 `docs/open-questions.md`를 삭제한다.
- 사용자 확정, Codex 구체화 초안, Codex 제안, 미정을 명확히 구분한다. 사용자가 확정하지 않은 제안을 확정이나 구현 완료로 기록하지 않는다.
- `필수 요구사항`, `포함`, `완료 조건`이라는 표현은 사용자 확정 범위에만 사용한다. 미확정 내용은 해당 절 가까이에 `구체화 초안`, `제안` 또는 `미정` 상태를 표시한다.
- 필수 체크 항목과 기능별 완료 조건이 확정되지 않았거나 깐깐한 관리자 검수가 승인하지 않았으면 요구사항을 `완성`으로 표시하지 않는다.
- 로그는 삭제·덮어쓰기 대신 추가한다. 기존 결정이 바뀌면 새 DEC 항목에서 이전 DEC와 변경 이유를 연결한다.
- 로그 항목은 결과 중심으로 짧게 쓴다. 원문, 대량 출력, 반복 설명을 복사하지 말고 관련 문서·DEC·CHG를 링크한다.
- 새 작업에서는 `docs/README.md`의 현재 상태와 해당 작업 행부터 확인하고, `rg`로 관련 제목·DEC·CHG를 찾은 뒤 필요한 범위만 읽는다. 같은 턴에서 바뀌지 않은 문서를 반복해 읽지 않고 전체 감사가 필요할 때만 전체를 읽는다.
- 상세 체크리스트는 해당 작업에서 실제로 필요할 때만 열고, 기능별 특수 규칙이 반복될 때만 그 코드에 가까운 하위 `AGENTS.md`를 추가한다. 루트 지침과 같은 내용을 하위 지침이나 문서에 복사하지 않는다.

## 개발과 검토

- 코드는 A·B·C 기능 중심 폴더로 나누고, 각 기능 안에서 View·ViewModel·Model 역할을 분리하며 상태는 `사용자 행동 → 상태 변경 → 화면 갱신`의 단방향으로 흐르게 한다.
- 하나의 작업·브랜치·PR은 하나의 검증 가능한 목적만 담는다. 기능, 대규모 리팩토링, 무관한 포맷 변경을 섞지 않는다.
- 작업 단위가 끝나면 사용자에게 변경 파일, 행동 변화, 검증 결과, 미확인 사항을 보여준다.
- 코드 변경 후에는 관련 포맷, 린트, 타입 검사, 테스트, 빌드를 실행하고 실제 실행 결과만 기록한다.
- 이름은 의도를 드러내고, 함수·모듈은 하나의 역할을 갖게 하며, 중복과 불필요한 추상화를 피한다. 리팩토링은 행동 보존 테스트와 함께 하고 가능하면 기능 PR과 분리한다.
- GitHub에 올리는 모든 변경은 `codex/` 브랜치의 작은 PR로 제출하고 `main`에 직접 푸시하지 않는다. PR은 `docs/pull-request-guide.md`와 공통 템플릿을 따르며, 병합은 사용자가 diff와 검증 결과를 확인한 뒤 결정한다.
- PR을 만들거나 갱신하기 전에 `.github/pull_request_template.md`의 모든 섹션을 실제 변경과 검증 결과로 채운다. 해당 사항이 없으면 `해당 없음`과 이유를 적는다.
- 실행하지 않은 테스트·타입 검사·린트·빌드·실기기 검증을 완료로 체크하지 않는다. Draft에는 `작업 중` 또는 `검증 예정`을 적고, 병합 가능한 PR에는 빈 템플릿 주석이나 설명 없는 자리표시자를 남기지 않는다.
- GitHub Issue 생성과 배포는 사용자의 명시적 요청이나 승인 후에만 수행한다.

## 병렬 작업과 코드 리뷰

- 병렬화는 코드 탐색, 테스트, 로그 분석, 리뷰처럼 서로 독립적인 읽기 중심 작업을 우선한다. 같은 파일을 동시에 수정하는 작업은 피한다.
- 병렬 작업 전에 작업 경계, 소유 파일, 종료 조건, 통합 및 검증 담당을 정한다. 독립 Git 저장소가 준비된 뒤 쓰기 작업은 별도 worktree/브랜치에서만 나눈다.
- 코드 리뷰는 정확성, 회귀, 누락된 테스트, 보안·개인정보, 오디오 생명주기 위험을 우선한다. 포맷·린트·타입 검사는 CI에 맡기고, AI 리뷰를 테스트·브랜치 보호·사용자 승인의 대체물로 사용하지 않는다.
- 검수자는 구현자의 의도를 선의로 보완하지 않는 깐깐한 관리자 역할로 검토한다. 증거가 없거나 사용자 확정과 어긋나면 완료를 반려하고, 오류를 교정한 뒤 같은 기준으로 다시 검증한다.

## Code Review Rules

- 사용자 확정 `REQ-*`·`DEC-*`와 PR diff를 대조한다. 미확정 제안을 구현했거나 PR 목적 밖 변경을 섞었으면 승인하지 않는다. 안전한 경로는 변경을 분리하고 사용자 확인 후 다시 제출하는 것이다.
- PR 설명과 체크박스를 검증 증거로 간주하지 않는다. 실제 CI 결과, 실행 명령과 결과, 필요한 Android 실기기 결과를 확인한다. 필수 검증이 누락되거나 실패했으면 완료로 승인하지 않는다.
- 개인정보 노출, 데이터 손실·중복, 오디오 중단·백그라운드 재생 회귀, 근거 없는 테스트 약화를 우선 확인한다. 발견 사항은 위치·재현 조건·영향·안전한 수정 방향과 함께 보고하고, 수정 후 같은 기준으로 재검수한다.

## 사용자와의 의사결정

- 한 번에 하나의 중요한 선택을 묻고, 선택지의 차이와 추천안을 설명한다.
- 사용자의 표현을 가능한 그대로 보존하고 Codex가 추가한 구체화는 별도로 표시한다.
